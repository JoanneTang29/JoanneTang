// 1. IMPORT REACT
import React from 'react';

// 2. ADDITIONAL IMPORTS
import Child from './Child';

// 3. CREATE COMPONENT
const Parent = () => {
  return (
    <div>
      <Child />
    </div>
  );
};

// 4. EXPORT COMPONEENT
export default Parent;
